<% include ./partials/header %>



<div class="container" style="margin: 10% 10%;border: 1px">
    <form action="/blogs/<%= blog._id%>?_method=PUT" method="POST">
        <div class="form-grou">
            <label for="blog[title]" class="col-form-label">Title</label>
            <div>
                <input type="text" name="blog[title]" value="<%= blog.title %>">
            </div>
        </div>
        <div class="form-group">
            <label for="blog[image]" class="col-form-label">Image</label>
            <div >
                <input type="text" name="blog[image]" value="<%= blog.image %>">
            </div>
        </div>
        <div class="form-group">
            <label for="blog[body]" class=" col-form-label">Content of the post</label>
            <div class="">
                <textarea name="blog[body]"  id="" cols="50" rows="8" ><%= blog.body %></textarea>
            </div>

        </div>
       <button class="btn btn-outline-info">Submmit!</button>
     </form>
</div>

<% include ./partials/footer %>